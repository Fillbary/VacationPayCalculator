# 🏖️ Калькулятор отпускных выплат  
  
Микросервис для расчета отпускных выплат сотрудников на основе средней заработной платы и продолжительности отпуска.  
## 📋 Требования  
  
    Java 11 или выше  
  
    Maven 3.6+  
  
    Spring Boot 2.7+  
  
## 🚀 Быстрый старт  
1\. Сборка приложения  
```bash  
mvn clean package  
```  
2\. Запуск приложения  
```bash  
java -jar target/vacation-pay-calculator-1.0.0.jar  
```  
Приложение будет доступно по адресу: http://localhost:8080  
## 📊 API Endpoints  
1\. Расчет по количеству дней  
  
GET /api/v1/calculate/days  
  
Рассчитывает сумму отпускных на основе количества дней отпуска без учета конкретных дат.  
  
Параметры:  
```java  
    averageSalary (required) - Средняя заработная плата за 12 месяцев (в рублях)  
  
    numberOfVacationDays (required) - Количество дней отпуска (1-28)  
```  
Пример запроса:  
```bash  
curl "http://localhost:8080/api/v1/calculate/days?averageSalary=50000&numberOfVacationDays=14"  
```  
Пример ответа:  
```json    
{  
  "vacationPayAmount": 23890.78,  
  "message": "Рассчитанная сумма отпускных за 14 дней"  
}  
```  
2\. Расчет по конкретным датам  
```java  
GET /api/v1/calculate/dates  
```  
Рассчитывает сумму отпускных на основе конкретных дат отпуска с учетом праздничных дней.  
  
Параметры:  
```java  
    averageSalary (required) - Средняя заработная плата за 12 месяцев (в рублях)  
  
    startDate (required) - Дата начала отпуска (формат: YYYY-MM-DD)  
  
    endDate (required) - Дата окончания отпуска (формат: YYYY-MM-DD)  
```  
Пример запроса:  
```bash  
curl "http://localhost:8080/api/v1/calculate/dates?averageSalary=50000&startDate=2023-06-01&endDate=2023-06-14"  
```  
Пример ответа:  
```json  
{  
  "vacationPayAmount": 17064.85,  
  "message": "Рассчитанная сумма отпускных за 10 рабочих дней"  
}  
```  
## 🧮 Формула расчета  
  
Отпускные рассчитываются по формуле:  
```text  
  
Отпускные = (Средняя зарплата / 29.3) × Количество оплачиваемых дней отпуска  
  
Где:  
  
    29.3 - среднемесячное количество календарных дней согласно Трудовому кодексу РФ  
  
    При расчете по датам праздничные дни исключаются из количества оплачиваемых дней  
```  
## ⚙️ Конфигурация  
Настройка праздничных дней  
  
Праздничные дни настраиваются в файле application.properties:  
```properties    
\# Формат properties  
vacation.holidays\[0\]=2023-01-01  
vacation.holidays\[1\]=2023-01-02  
vacation.holidays\[2\]=2023-01-07  
vacation.holidays\[3\]=2023-02-23  
vacation.holidays\[4\]=2023-03-08  
vacation.holidays\[5\]=2023-05-01  
vacation.holidays\[6\]=2023-05-09  
vacation.holidays\[7\]=2023-06-12  
vacation.holidays\[8\]=2023-11-04  
```  
Или в application.yml:  
```yaml    
vacation:  
  holidays:  
    - 2023-01-01  
    - 2023-01-02  
    - 2023-01-07  
    - 2023-02-23  
    - 2023-03-08  
    - 2023-05-01  
    - 2023-05-09  
    - 2023-06-12  
    - 2023-11-04  
```  
## 🧪 Тестирование  
Запуск unit-тестов  
```bash    
mvn test  
```  
Структура тестов  
```java  
    CalendarServiceTest - тесты сервиса работы с календарем  
  
    VacationPaymentServiceImplTest - тесты сервиса расчета отпускных  
```  
Примеры тестовых сценариев  
  
    Расчет отпускных по количеству дней  
  
    Расчет отпускных по датам с учетом праздников  
  
    Валидация некорректных входных данных  
  
    Граничные случаи (минимальный и максимальный отпуск)  
  
## 🏗️ Архитектура проекта  
```text    
src/main/java/com/example/vacationPayCalculator/  
├── config/  
│   └── HolidayConfiguration.java          # Конфигурация праздничных дней  
├── controller/  
│   └── CalculateController.java           # REST контроллер  
├── dto/  
│   ├── request/  
│   │   ├── CalculateByDaysRequestDTO.java # DTO запроса по дням  
│   │   └── CalculateByDatesRequestDTO.java # DTO запроса по датам  
│   └── response/  
│       ├── CalculateResponseDTO.java      # DTO успешного ответа  
│       └── ErrorResponseDTO.java          # DTO ошибки  
├── exception/  
│   ├── GlobalExceptionHandler.java        # Глобальный обработчик исключений  
│   └── VacationCalculationException.java  # Кастомное исключение  
└── service/  
    ├── CalendarService.java               # Сервис работы с календарем  
    ├── VacationPaymentService.java        # Интерфейс сервиса расчета  
    └── impl/  
        └── VacationPaymentServiceImpl.java # Реализация сервиса расчета  
```  
## 🔒 Валидация  
  
Приложение проверяет:  
  
    Средняя зарплата должна быть > 0  
  
    Количество дней отпуска: 1-28 дней  
  
    Дата начала отпуска не может быть позже даты окончания  
  
    Даты не могут быть в прошлом  
  
    В периоде отпуска должен быть хотя бы один рабочий день  
  
## 🚨 Обработка ошибок  
  
При возникновении ошибок приложение возвращает стандартизированный ответ:  
```json    
{  
  "timestamp": "2023-06-15 14:30:45",  
  "status": 400,  
  "error": "Bad Request",  
  "message": "Зарплата должна быть больше нуля",  
  "path": "/api/v1/calculate/days"  
}  
```  
HTTP статусы:  
  
    200 OK - успешный расчет  
  
    400 Bad Request - ошибка валидации входных данных  
  
    500 Internal Server Error - внутренняя ошибка сервера  
  
## 📈 Примеры использования  
Пример 1: Стандартный отпуск  
```text  
  
Запрос:  GET /api/v1/calculate/days?averageSalary=75000&numberOfVacationDays=21  
Ответ:   {"vacationPayAmount": 53737.20, "message": "Рассчитанная сумма отпускных за 21 день"}  
```  
Пример 2: Отпуск с учетом праздников  
```text  
  
Запрос:  GET /api/v1/calculate/dates?averageSalary=60000&startDate=2023-12-28&endDate=2024-01-10  
Ответ:   {"vacationPayAmount": 20477.82, "message": "Рассчитанная сумма отпускных за 10 рабочих дней"}  
```  
Пример 3: Минимальный отпуск  
```text  
  
Запрос:  GET /api/v1/calculate/days?averageSalary=30000&numberOfVacationDays=1  
Ответ:   {"vacationPayAmount": 1023.89, "message": "Рассчитанная сумма отпускных за 1 день"}  
```  
## 🛠️ Технологии  
  
    Java 11 - язык программирования  
  
    Spring Boot 2.7+ - фреймворк  
  
    Maven - сборка и зависимости  
  
    JUnit 5 - тестирование  
  
    Mockito - мокирование в тестах  
  
    Lombok - сокращение шаблонного кода  
  
    Jakarta Validation - валидация данных  

